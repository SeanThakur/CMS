<h2>Update Categories</h2>
<br>
<div class="container">
    <div class="row">
        <div class="col-sm-4">
            <div class="card">
                <div class="card-body">
                    <form action="/Videojet.admin/categories/edit/{{category.id}}?_method=PUT" method="POST">
                        <div class="form-group">
                            <label for="name">Name: </label>
                            <input required class="form-control" name="name" id="name" placeholder="Update Categories" value="{{category.name}}">
                        </div>
                        <button type="submit" class="btn btn-block btn-primary">
                            Update
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>